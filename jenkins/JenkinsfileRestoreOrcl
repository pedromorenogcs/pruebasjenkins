pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch:'main', url: 'https://github.com/pedromorenogcs/pruebasjenkins.git'
            }
        }
         stage('Ansible') {
             steps {
                  ansiblePlaybook playbook: 'ansible/oracleRestore/restore.yaml'
            }
        }
        
    }
}
